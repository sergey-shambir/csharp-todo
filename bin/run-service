#!/usr/bin/env bash

set -o errexit

PARENT_DIR=$(dirname "$(readlink -f "$0")")
PROJECT_DIR=$(dirname "$PARENT_DIR")
CONTENT_ROOT_DIR=$PROJECT_DIR/frontend

exec dotnet run --project TodoApi/ --launch-profile https -- "$CONTENT_ROOT_DIR"
